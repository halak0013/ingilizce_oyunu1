import 'package:flutter/material.dart';

class Kurallar extends StatelessWidget {
  const Kurallar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Faster Speaking Kuralları")),
      body: Container(),
    );
  }
}
